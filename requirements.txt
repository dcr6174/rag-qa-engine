# Core (API + evaluation)
fastapi>=0.110
pypdf>=4.0  # PDF ingestion with page-anchored citations (pure Python)
uvicorn[standard]>=0.29
pydantic>=2.6
numpy>=1.26

# Optional: higher-quality neural embeddings.
# The project runs fully offline without this package (hashing embedder fallback).
# sentence-transformers>=2.7

# Optional: LLM generation via any OpenAI-compatible endpoint (OpenAI, Azure OpenAI, Ollama, vLLM).
# openai>=1.30

# Dev
pytest>=8.0
httpx>=0.27
